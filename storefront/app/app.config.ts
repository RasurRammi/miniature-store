const defaultColors = {
  primary: 'green',
  neutral: 'slate',
}
const nomnomColors = {
  primary: 'nomnom-main',
  secondary: 'nomnom-accent',
  neutral: 'nomnom-background',
}

export default defineAppConfig({})
